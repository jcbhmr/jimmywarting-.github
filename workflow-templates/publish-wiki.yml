name: Publish wiki
on:
  push:
    branches: [$default-branch]
    paths: [wiki/**, .github/workflows/publish-wiki.yml]
jobs:
  publish-wiki:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: jcbhmr/publish-wiki@v1
